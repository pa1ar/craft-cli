# craft-cli 0.5.0 release

Issue: LABS-262

## Goal

Ship the space-wide task explorer as craft-cli 0.5.0, update the bundled skill and changelog, publish the GitHub release, and publish a branded update on 1ar.io.

## Scope

- Promote the existing Unreleased task explorer entry to 0.5.0.
- Confirm the bundled `skill/SKILL.md` documents all-space task discovery and filters.
- Run unit tests, typecheck, build, and focused credentialed task integrations.
- Tag and publish v0.5.0.
- Generate and verify a 1ar-branded update image through the registered Animate project.
- Add and build the 1ar.io update from a clean worktree, push it, and verify the live URL.
- Close LABS-262 and leave the vault trace.

## Verification

- `bun test tests/unit`
- `bun run typecheck`
- `bun run build`
- Focused task integration tests with live Craft credentials
- `gh release view v0.5.0`
- `bun run build` in the isolated 1ar-astro worktree
- Production update URL returns the published article and image

## Result

- Released `v0.5.0` from commit `6255913` and published the GitHub release.
- Verified the rebuilt CLI against the live Craft space and confirmed document tasks through the all-space default.
- Published the 1ar.io update and 1200×630 image from site commit `441a918`.
- Verified the production article and byte-identical image after the Vercel deployment completed.
